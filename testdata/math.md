Inline math: $E = mc^2$ and a Greek pair $\alpha + \beta$.

Display math on its own line:

$$\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}$$

A matrix, to cover a construct that nests:

$$\begin{pmatrix} a & b \\ c & d \end{pmatrix}$$

Text after the formulas, so the surrounding document is exercised too.
